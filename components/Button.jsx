import React from 'react'

function Button({open__dialog}) {
    return (
        <div>
            <Button variant="danger">Danger</Button> <Button variant="info">Open dialog</Button>{' '}
        </div>
    )
}

export default Button
